#include <bits/stdc++.h>
#define endl "\n"
// #define int long
// #define long long long
const int MOD = (int)1e9 + 7;
const int limit = 1000001;
using namespace std;
#ifndef ONLINE_JUDGE
    clock_t tStart = clock();
#endif

void runtime(){
    #ifndef ONLINE_JUDGE
        cerr << "\n>> Runtime: " << (double)clock() / CLOCKS_PER_SEC << "s\n";
    #endif
}

int n, m, k;
int f[10001][10001] = {0};

int bfs(){
    queue <pair<int,int>> q;
    f[0][0] = 1;
    q.push({0,0});
    while(!q.empty()){
        int x = q.front().first, y = q.front().second;
        q.pop();
        int dx[6] = {0, x, n, x, max(0, x+y-m), min(x+y, n)};
        int dy[6] = {y, 0, y, m, min(x+y, m), max(0, x+y-n)};
        for(int i = 0; i < 6; i++){
            if(f[dx[i]][dy[i]] == 0){
                f[dx[i]][dy[i]] = f[x][y]+1;
                if(dx[i] == k || dy[i] == k) return (f[dx[i]][dy[i]]-1);
                q.push({dx[i], dy[i]});
            }
        }
    }
    return -1;
}
/*
struct State {
    int x, y, step;

    State(int x, int y, int step) : x(x), y(y), step(step) {}

    bool operator<(const State& other) const {
        return x < other.x || (x == other.x && y < other.y);
    }
};

int bfs(int n, int m, int k) {
    queue<State> q;
    set<State> visited;

    State initialState(0, 0, 0);
    q.push(initialState);
    visited.insert(initialState);

    while (!q.empty()) {
        State current = q.front();
        q.pop();

        if (current.x == k || current.y == k) {
            return current.step;
        }

        // Đổ hết nước từ bình 1 vào bình 2
        State nextState1(0, current.y, current.step + 1);
        if (visited.find(nextState1) == visited.end()) {
            q.push(nextState1);
            visited.insert(nextState1);
        }

        // Đổ hết nước từ bình 2 vào bình 1
        State nextState2(current.x, 0, current.step + 1);
        if (visited.find(nextState2) == visited.end()) {
            q.push(nextState2);
            visited.insert(nextState2);
        }

        // Đổ đầy nước vào bình 1
        State nextState3(n, current.y, current.step + 1);
        if (visited.find(nextState3) == visited.end()) {
            q.push(nextState3);
            visited.insert(nextState3);
        }

        // Đổ đầy nước vào bình 2
        State nextState4(current.x, m, current.step + 1);
        if (visited.find(nextState4) == visited.end()) {
            q.push(nextState4);
            visited.insert(nextState4);
        }

        // Đổ nước từ bình 1 vào bình 2
        int pourAmount = min(current.x, m - current.y);
        State nextState5(current.x - pourAmount, current.y + pourAmount, current.step + 1);
        if (visited.find(nextState5) == visited.end()) {
            q.push(nextState5);
            visited.insert(nextState5);
        }

        // Đổ nước từ bình 2 vào bình 1
        pourAmount = min(n - current.x, current.y);
        State nextState6(current.x + pourAmount, current.y - pourAmount, current.step + 1);
        if (visited.find(nextState6) == visited.end()) {
            q.push(nextState6);
            visited.insert(nextState6);
        }
    }

    return -1; // Không đổ được k lít nước
}
*/
/*
struct State {
    int x, y, steps;

    State(int _x, int _y, int _steps) : x(_x), y(_y), steps(_steps) {}

    // Định nghĩa toán tử == để so sánh trạng thái
    bool operator==(const State &other) const {
        return x == other.x && y == other.y;
    }

    // Định nghĩa hàm hash để sử dụng trong unordered_set
    struct Hash {
        size_t operator()(const State &state) const {
            return hash<int>()(state.x) ^ hash<int>()(state.y);
        }
    };
};

int bfs(int n, int m, int k) {
    // Khởi tạo hàng đợi và tập trạng thái đã duyệt qua
    queue<State> q;
    unordered_set<State, State::Hash> visited;

    // Thêm trạng thái ban đầu vào hàng đợi
    q.push(State(0, 0, 0));
    visited.insert(State(0, 0, 0));

    // Bắt đầu duyệt BFS
    while (!q.empty()) {
        State current = q.front();
        q.pop();

        // Nếu đạt được lượng nước cần đổ, trả về số bước
        if (current.x == k || current.y == k)
            return current.steps;

        // Thử các phương án đổ nước từ một bình sang bình khác
        // Đổ từ bình 1 sang bình 2
        if (current.x > 0) {
            State newState(0, current.y, current.steps + 1);
            if (visited.find(newState) == visited.end()) {
                q.push(newState);
                visited.insert(newState);
            }
        }
        // Đổ từ bình 2 sang bình 1
        if (current.y > 0) {
            State newState(current.x, 0, current.steps + 1);
            if (visited.find(newState) == visited.end()) {
                q.push(newState);
                visited.insert(newState);
            }
        }
        // Đổ đầy bình 1
        if (current.x < n) {
            State newState(n, current.y, current.steps + 1);
            if (visited.find(newState) == visited.end()) {
                q.push(newState);
                visited.insert(newState);
            }
        }
        // Đổ đầy bình 2
        if (current.y < m) {
            State newState(current.x, m, current.steps + 1);
            if (visited.find(newState) == visited.end()) {
                q.push(newState);
                visited.insert(newState);
            }
        }
        // Đổ từ bình 1 sang bình 2 cho đến khi bình 1 hết hoặc bình 2 đầy
        int pour = min(current.x, m - current.y);
        State newState(current.x - pour, current.y + pour, current.steps + 1);
        if (visited.find(newState) == visited.end()) {
            q.push(newState);
            visited.insert(newState);
        }
        // Đổ từ bình 2 sang bình 1 cho đến khi bình 2 hết hoặc bình 1 đầy
        pour = min(n - current.x, current.y);
        newState = State(current.x + pour, current.y - pour, current.steps + 1);
        if (visited.find(newState) == visited.end()) {
            q.push(newState);
            visited.insert(newState);
        }
    }

    // Nếu không tìm thấy cách để đổ được k lít nước
    return -1;
}
*/

/*
int BFS(int n,int m,int k)  //so buoc it nhat
{
    queue<pair<int,int> > Q;
    map<pair<int,int>,int> M;
    M[{0,0}]=1;
    Q.push({0,0});
    while(Q.size())
    {
        int x=Q.front().first,y=Q.front().second,z=x+y;
        Q.pop();
        pair<int,int> Next[]={{0,y},{n,y},{x,0},{x,m},{max(0,z-m),
        min(z,m)},{min(z,n),max(0,z-n)}};
        for(auto v:Next)
        if(M[v]==0) //chua co 
        {
            M[v]=M[{x,y}]+1;
            Q.push(v);
            if(v.first==k||v.second==k) return M[v]-1;
        }
    }
    return -1;
}
*/

void sol() {
    // int n, m, k;
    cin >> n >> m >> k;

    // int steps = bfs(n, m, k);
    int steps = bfs();
    if (steps == -1)
        cout << "Khong dong duoc nuoc";
    else
        cout << steps;
}

main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    sol();

    runtime();
    return 0;
}

/*⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
			 ⠀⠀⠀⠀⠀⣀⣀⣀⣀⣀⣀⣀⣀⣀⣀⣀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
		 ⠀⠀⠀⠀⠀⠀⢀⣠⡴⠖⠋⠉⠁⠀⠀⠉⠁⠀⠀⠀⠉⠉⠉⠓⠶⢤⣀⡀⢀⣞⢹⡀⠀⣠⡦⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀    ⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢀⣠⠖⠋⠁⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠰⣄⡀⠈⠙⠿⣿⣿⡇⣰⣯⡇⠀⠀⣀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⣠⠞⠋⠀⠀⢀⣤⠞⠋⠀⢠⠆⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠈⠻⣆⠀⠀⠈⠻⣿⣿⣟⣇⡴⢻⡟⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢀⣴⠟⠁⠀⠀⠀⣰⠏⠀⠀⠀⢠⡟⠀⠀⠀⠀⠀⠀⠀⠀⢀⠀⠀⠀⠀⠀⠈⠃⠀⠀⠀⠘⢯⣿⠟⣴⣿⠤⣤⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠘⠛⠿⠿⣾⠇⠀⠀⢠⡾⠁⠀⢀⡀⢀⣾⠀⠀⠀⠀⠀⠀⠀⠀⢠⠏⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⣨⢿⣰⣿⠃⠀⠈⢳⡀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⡼⠁⠀⠀⣰⠏⠀⠐⠋⠉⣩⡟⣿⢦⠀⠀⠀⠀⠀⠀⣠⣿⡶⠶⠶⠶⢤⡀⠀⠀⠀⠀⢀⢜⣽⣿⢿⣽⠀⠀⠀⠀⢳⡀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⢠⣼⣁⣀⣠⠞⠁⢠⣄⡤⠶⠾⠿⢤⣸⡄⠀⠀⠀⠀⠠⡾⠁⠈⣧⣀⣤⣀⣀⠹⠆⠀⠀⠐⢧⡺⣥⡽⣣⣿⠀⠀⠀⠀⠈⣧⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠈⣷⠦⠤⣤⣤⣴⣾⠏⣰⠛⠳⡆⠀⠹⣿⡄⠀⠀⣇⡾⠁⢠⡞⠉⢀⣤⣄⠉⢳⡴⠀⡇⠀⠀⠱⣭⣾⠛⡟⠀⠀⠀⠀⠀⢹⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⣰⢿⠀⠀⠀⠀⣿⣏⠀⠹⠷⠾⠃⠀⠀⣿⠙⢦⡀⡿⠀⠀⣿⠀⠀⢿⣄⣸⡇⠀⢷⠀⡇⠀⠀⣠⢼⣾⡄⣿⣤⣀⠀⠀⠀⢸⡆⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⢠⠏⢈⣷⣠⡀⠀⢸⡼⣆⠀⠀⠀⠀⠀⣰⠏⠀⠀⠙⢷⠀⠀⢿⡀⠀⠀⠉⠁⠀⢀⡟⢻⡇⠀⠀⢻⢸⢿⣿⣿⠚⠛⠀⠀⠀⠈⡇⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⢀⠟⢀⡞⢸⡿⢿⠶⢶⠿⠏⠛⠶⠤⠶⠛⠁⠀⠀⠀⠀⠀⠀⠀⠀⠳⢦⣀⣀⣀⡴⠟⢀⡼⠁⠀⠀⠈⣿⣆⢹⣿⡆⠀⠀⠀⠀⠀⡇⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⣾⢀⡟⠀⢸⡇⠀⢰⡏⠀⠀⠀⠀⠀⠀⠀⠀⠀⣦⣀⣀⣀⡶⠀⠀⠀⠀⠀⠉⠀⠀⣠⠞⠁⢀⣠⣀⡼⠧⣌⣙⢻⠁⠀⠀⠀⠀⢰⡇⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⣇⣼⠀⠀⠘⡇⠀⠘⣧⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠉⠉⠁⠀⠀⠀⠀⠀⠀⠀⡴⢾⡷⢶⣺⡿⣿⠯⣄⣀⣀⣉⣛⣶⣦⠄⠀⠀⢸⠁⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠙⢿⣦⠀⠀⢧⠀⢠⡌⠳⢦⣀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢀⡼⠁⠈⠁⢠⡇⠀⠀⠈⠉⢉⡏⠀⠀⠀⠀⠀⣼⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠘⣆⠀⢷⣄⠀⠈⢿⠓⣦⣤⣀⣀⠀⠀⠀⠀⠀⠀⠀⠀⣀⣀⣠⡞⣁⡴⠀⢠⡞⠀⠀⠀⠀⠀⣾⠀⠀⠀⠀⠀⠀⡟⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⣀⣈⣳⣬⣿⣟⣶⢬⣷⣽⣯⣥⣭⣿⡛⣻⢟⣿⣿⡟⠉⢛⣻⣿⣿⠏⢀⣴⣯⣤⣀⠀⣀⣀⣰⠃⠀⠀⠀⠀⠀⢠⠇⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⣿⣳⣿⣿⣿⣿⡿⣫⡟⠀⣽⣿⣿⣿⣿⣿⣿⣻⣿⣿⣿⣿⣿⣿⣿⡟⣁⡴⢿⣿⣿⣿⣿⣿⣿⣿⠏⠀⠀⠀⠀⠀⠀⣸⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⡖⠛⠛⠋⠙⣿⣿⠟⣺⣟⣁⣠⣿⣿⣿⣿⣿⣿⣿⣿⣟⣿⣿⣿⣿⣿⣿⣿⣿⣧⣾⣿⡟⠛⣿⣿⣿⠛⠙⢲⡄⠀⠀⠀⠀⡟⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠻⣄⣀⠀⠀⠘⣿⡾⠻⣏⣽⡟⢻⡿⠋⣴⣻⢿⣿⣾⣿⣿⣷⠉⠻⢟⣿⣿⡇⠈⢻⣿⡷⣶⣿⠟⣁⣀⣤⠞⠀⠀⠀⠀⢰⠇⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠯⠤⠖⠚⠛⢧⣀⡿⠃⠀⣼⠀⠀⢹⣿⣾⣿⣿⣿⣿⡿⠀⠀⠀⠙⠻⢧⡀⠀⢻⣽⡿⣧⣀⣸⠇⠀⠀⠀⠀⠀⠀⣼⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠙⠁⢀⡼⠃⠀⠀⣼⡟⢻⡻⢿⡿⣟⣇⠀⠀⠀⠀⠀⠀⠙⢿⡉⠁⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢠⠏⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢀⡾⠁⠀⠀⣸⣿⠁⢰⣷⣯⡇⠹⡿⡄⠀⠀⠀⠀⠀⠀⠀⠹⢦⡀⣀⣠⠞⠃⠀⢀⣴⡗⠀⡾⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢠⡞⠀⠀⠀⢰⣿⠃⠀⢨⢿⣿⡄⠀⢻⣿⡀⠀⠀⠀⠀⠀⠀⠀⠀⠹⢯⣀⡤⠴⠞⠋⣸⢁⡾⠁⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⣠⠏⠀⠀⠀⢠⣿⡇⠀⠀⠘⢿⣿⠃⠀⠀⢿⣷⡀⠀⠀⠀⠀⠀⠀⠀⠀⠈⣿⣦⡀⠀⢰⣿⠟⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢀⣴⢻⣄⠀⠀⢀⣾⣾⠀⠀⠀⣴⡋⠙⣦⠀⠀⠈⣿⣷⣄⠀⠀⠀⠀⠀⠀⣀⡴⠃⢈⣻⡦⠈⠁⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢸⣿⢾⣷⣶⣿⣻⣿⣦⣄⣀⣠⠏⠸⣏⡀⠀⣀⣿⣏⣻⣷⡖⢒⣛⣿⣯⡵⠚⢿⡉⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⡏⣹⣆⣸⣿⣿⣿⣿⡿⠦⠬⢭⣭⣍⣩⣭⠭⠽⣿⣿⣿⣟⡯⠭⣎⠛⢿⣇⠀⣰⢿⡀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⢠⡿⢿⣿⣍⠛⣿⠿⠿⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠈⠙⠿⠞⠳⣶⠽⠾⠯⠹⠟⠛⠋⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠘⢧⣀⣨⣿⠟⠁⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠉⠉⠁⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀⠀
*/
